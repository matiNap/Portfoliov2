import React from "react";

interface Props {
  isHidden: boolean;
}

const Footer = ({ isHidden }: Props) => {
  return (
    <footer>
      <div>test</div>
    </footer>
  );
};

export default Footer;
